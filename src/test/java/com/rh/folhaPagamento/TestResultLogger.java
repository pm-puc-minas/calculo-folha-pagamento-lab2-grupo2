// Arquivo: TestResultLogger.java

/*
package com.rh.folhaPagamento;

import org.junit.jupiter.api.extension.ExtensionContext;
import org.junit.jupiter.api.extension.TestWatcher;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

public class TestResultLogger implements TestWatcher {

    private static final Logger logger = LoggerFactory.getLogger(TestResultLogger.class);

    @Override
    public void testSuccessful(ExtensionContext context) {
        logger.info("✔️ SUCESSO: Teste [{}] concluído!", context.getDisplayName());
    }

    @Override
    public void testFailed(ExtensionContext context, Throwable cause) {
        logger.error("❌ FALHA: Teste [{}] falhou. Causa: {}",
                context.getDisplayName(),
                cause.getMessage()
        );
    }
}

// refatoracao futura, alterar para mostrar falha na log e tb nao repetir no codigo a mensagem que deve ser exibida.
 */